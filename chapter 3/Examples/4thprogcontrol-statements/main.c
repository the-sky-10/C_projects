#include <stdio.h>

int main()
{
    int n;
    printf("enter the number\n");
    scanf("%d",&n);
    if(n%2==0)
        printf("enter the given number is even");
    else
        printf("enter the givem number is odd");

    return 0;
}
